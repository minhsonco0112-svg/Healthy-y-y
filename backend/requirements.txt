# Kotai Voice Agent — Python dependencies
# Install: pip install -r requirements.txt

# --- LiveKit real-time agent framework + plugins ---
livekit-agents==1.2.11
livekit-plugins-openai==1.2.11          # Groq-compatible LLM client
livekit-plugins-silero==1.2.11          # Silero VAD
livekit-plugins-turn-detector==1.2.11   # turn detection

# --- Speech-to-Text: faster-whisper (CTranslate2), CPU ---
faster-whisper==1.2.0
ctranslate2==4.6.1

# --- Text-to-Speech: Kokoro TTS (neural, local) ---
torch>=2.0
pydub>=0.25.1

# --- Utilities ---
openai==1.108.1
python-dotenv==1.0.1
httpx==0.28.1
pydantic>=2.0
numpy>=1.0
